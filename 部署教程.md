# 阿平仓库管理系统 - Render 部署教程

## 📋 准备工作

在开始之前，你需要准备以下账号（都是免费的）：

1. **GitHub 账号** - 用来存放代码
   - 注册地址：https://github.com/join
   
2. **Render 账号** - 用来部署应用
   - 注册地址：https://dashboard.render.com/register
   - 建议用 GitHub 账号登录，更方便

---

## 🚀 第一步：把代码上传到 GitHub

### 方法一：最简单 - 上传 ZIP 包（推荐小白用）

1. 下载我给你的 `aping-warehouse.zip` 文件
2. 打开 https://github.com/new （登录你的 GitHub 账号）
3. 填写信息：
   - **Repository name**: `aping-warehouse`（仓库名，随便起）
   - **Public** 选这个（公开的，免费）
   - 勾选 **Add a README file**（可选）
4. 点击 **Create repository**（创建仓库）
5. 进入刚创建的仓库页面
6. 点击 **Add file** → **Upload files**
7. 把 zip 包里的所有文件拖进去（注意：是文件，不是整个 zip）
8. 点击底部 **Commit changes**（提交更改）

### 方法二：用 Git 命令（如果你会的话）

```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/你的用户名/aping-warehouse.git
git push -u origin main
```

---

## 🚀 第二步：在 Render 上部署

### 1. 登录 Render
打开 https://dashboard.render.com/ ，用 GitHub 账号登录

### 2. 创建 Web Service
- 点击右上角 **New +** 
- 选择 **Web Service**

### 3. 连接 GitHub 仓库
- 找到你刚创建的 `aping-warehouse` 仓库
- 点击右边的 **Connect** 按钮

### 4. 填写配置信息

按照下面的说明填写：

| 配置项 | 填什么 |
|--------|--------|
| **Name** | `aping-warehouse`（随便起，会变成你的网址） |
| **Region** | 选 `Singapore`（新加坡，离国内近，速度快） |
| **Runtime** | 选 `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `node server.js` |
| **Instance Type** | 选 **Free**（免费版） |

### 5. 添加环境变量（重要！）

往下滚，找到 **Environment Variables** 部分，点击 **Add Environment Variable**：

| Key | Value |
|-----|-------|
| `DATA_DIR` | `/var/data` |
| `NODE_VERSION` | `18.17.0` |

### 6. 添加磁盘（保存数据用，必须加！）

继续往下滚，找到 **Disk** 部分，点击 **Add Disk**：

| 配置项 | 填什么 |
|--------|--------|
| **Name** | `data` |
| **Mount Path** | `/var/data` |
| **Size (GB)** | `1`（免费版够用了） |

### 7. 开始部署

点击底部的 **Create Web Service** 按钮

然后等待 2-5 分钟，看到页面显示 **Live** 就成功了！

---

## ✅ 第三步：访问你的应用

部署成功后，Render 会给你一个网址，格式是：
```
https://你的应用名.onrender.com
```

比如：`https://aping-warehouse.onrender.com`

在浏览器打开这个网址，就可以用了！

**默认登录账号：**
- 用户名：`admin`
- 密码：`123456`

---

## ⚠️ 注意事项

### 关于免费版
- Render 免费版如果 15 分钟没人访问会"休眠"
- 第一次打开可能要等 30 秒左右（唤醒中）
- 唤醒后就正常了
- 数据会保存在磁盘里，不会丢

### 关于数据安全
- 所有数据都存在 Render 的磁盘上
- 多人使用时，数据是实时同步的
- 建议定期在应用里导出数据备份

### 想升级的话
- 如果用的人多，可以升级到付费版（每月约 7 美元）
- 付费版不会休眠，速度也更快

---

## 🆘 常见问题

**Q: 部署失败了怎么办？**
A: 看 Render 页面的日志（Logs），把错误信息截图发给我，我帮你看。

**Q: 网站打开很慢？**
A: 免费版第一次打开要等 30 秒唤醒，之后就快了。

**Q: 数据会丢吗？**
A: 只要不删除磁盘，数据就不会丢。建议定期在应用里导出备份。

**Q: 怎么改密码？**
A: 登录后，在"系统设置"里可以改。

**Q: 能加多少用户？**
A: 没有限制，你可以在"用户管理"里添加。

---

## 📞 需要帮助？

如果部署过程中遇到问题，把错误截图发给我，我帮你解决！
